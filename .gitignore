target/
!.mvn/wrapper/maven-wrapper.jar
.idea/
*.iml
.vscode/
*.class
*.log

# Secrets & local config
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Demo source videos (MP4 etc. — put under docs/internal/; GIF is committed)
*.mp4
*.mov
*.avi
*.wmv

# Maven / Java build artifacts outside target
*.jar
!.mvn/wrapper/maven-wrapper.jar
*.war
*.ear
hs_err_pid*
replay_pid*

# Editor swap
*.swp
*~

# Internal dev docs (local only, not for public repo)
docs/internal/
docs/superpowers/

# Agent skills (local only — use @git-ship-feature in Cursor)
.cursor/skills/

# RAG uploads (local only)
uploads/rag/
